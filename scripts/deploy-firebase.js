#!/usr/bin/env node

console.error('Partial Firebase deployment is disabled.');
console.error('Prepare an isolated release, then run npm run production:deploy with its exact commit confirmation.');
process.exit(1);
