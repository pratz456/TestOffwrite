# WriteOff Written Information Security Program (WISP)

Date: 2026-09-24. Version: 1.3 (reviewed against live production release `a219cbe` and the subsequent merged security candidate; v1.2 dated 2026-09-21). Owner: Qualified Individual (see §2). Review: at least annually and after any material change.

Basis: FTC Standards for Safeguarding Customer Information, 16 CFR Part 314 (as amended 2021; breach notification added 2023, effective May 2024). Tax preparation firms are listed as "financial institutions" in §314.2(h); a person who develops software used to prepare or file returns is a "tax return preparer" under Treas. Reg. §301.7216-1(b)(2)(i)(B). IRS Pub 4557 and Pub 5708 (WISP template) were used as checklists. Controls verified in the live project are identified as live; later candidate controls are not treated as deployed until their coordinated release. Claims that could not be verified from code or a primary source are marked unverified.

Small-institution note: §314.6 exempts institutions that maintain customer information on fewer than 5,000 consumers from some program elements (FTC compliance guide). The exact list of exempted paragraphs was not verified against the eCFR text in this pass (site blocked automated retrieval); WriteOff adopts all nine elements regardless, and the breach-notification duty in §314.4(j) applies at any size.

## 1. Scope and objectives (§314.3)

Customer information in scope: everything a user gives WriteOff or that WriteOff derives to analyze self-employment deductions: login identity, profile and business facts, bank account and transaction data obtained through Plaid, receipt images and extracted text, tax organizer answers (including SSNs), imported W-2/1099 data, AI analysis outputs, billing status, support requests, and operational logs. Objectives: confidentiality and integrity of that information, protection against anticipated threats, and prevention of unauthorized access that could cause substantial harm to a user.

Systems in scope: Firebase project `writeoff-23910` (Hosting, Cloud Run SSR service `ssrwriteoff23910` in `us-central1`, Firestore, Cloud Storage, Firebase Authentication, Cloud Functions `functions` and `functions-analysis` in `us-central1`), the GitHub repository and Actions runners, operator workstations, and the vendor services listed in `docs/compliance/SUBPROCESSORS_2026-09-17.md`.

Companion procedures adopted with this program: access control (`docs/compliance/ACCESS_CONTROL_POLICY_2026-09-21.md`), retention and disposal (`docs/compliance/DATA_RETENTION_AND_DELETION_2026-09-17.md`), and incident response (`docs/compliance/INCIDENT_RESPONSE_PLAN_2026-09-17.md`). Each is reviewed on the same cycle as this program.

## 2. Qualified Individual (§314.4(a))

Role: one named person accountable for implementing and supervising this program, approving encryption alternatives and MFA equivalents in writing, receiving incident reports, and reporting at least annually to the owner (§9). May be an employee or a contractor; if a contractor, WriteOff still designates a senior person to oversee them.

Status: designated on 2026-09-21 (the CEO). The name, date and contact are recorded in the operator-held appendix to this document, not in the public repository; the appendix accompanies copies of this program shared with vendors.

## 3. Risk assessment (§314.4(b))

### 3.1 Data inventory (field → storage → encryption at rest / in transit → who can read → retention)

| Data | Where | At rest | Who can read | Retention |
|---|---|---|---|---|
| Login identity: email, password hash or Google account link, email-verified flag, custom `admin` claim | Firebase Authentication | Google-managed encryption; TLS in transit | The user; server via Admin SDK | Until account deletion (`adminAuth.deleteUser`) |
| Session: `__session` cookie (14 days, httpOnly, secure, SameSite=Lax) | Browser + Firebase session cookie | Signed cookie; TLS | Holder of the cookie | 14 days or logout; revocation is checked on every API call (`checkRevoked = true`) |
| Profile: name, email, state, profession, filing status, income, entity type, business purpose, NAICS, EIN, home-office sq ft, vehicle business %, W-2 income, consent record | Firestore `user_profiles/{uid}` | Google default encryption; candidate stores EIN as AES-256-GCM ciphertext plus last four and migrates legacy plaintext before client reads | Owner; server. Clients cannot write subscription, consent or EIN storage fields | Until deletion (recursive delete of the profile tree) |
| Bank access tokens | Firestore `plaid_connections/{itemId}` | AES-256-GCM at application layer (`PLAID_TOKEN_ENCRYPTION_KEY`, random IV) plus Google default encryption | Server only; rules deny all client access | Until disconnect (`/item/remove` at Plaid, token field removed) or deletion |
| Bank accounts and transactions: institution, masked account, merchant, amount, date, city/state, MCC, category, payment channel, user notes and business-purpose fields | Firestore `user_profiles/{uid}/accounts/{id}/transactions/{txId}` and `transactions` | Google default encryption | Owner (read; update limited to user-editable fields); server | Until deletion |
| AI outputs: `deduction_score`, `ai_analysis`, job status | Transaction documents; `analysis_jobs`, `analysis_status`, `analysis_tasks` | Google default encryption | Owner read-only; server writes | Until deletion |
| Receipt images and OCR text | Cloud Storage `receipts/{uid}/…`; Firestore `receipts` | Google default encryption; ≤10 MB; content type and byte checks | Authenticated API after verified-email and transaction-ownership checks; candidate denies direct client Storage access | Until deletion (prefix delete) |
| Tax organizer answers | Firestore `tax_organizers` | `taxpayerSSN`, `spouseSSN`, `bankAccount`, `bankRouting`, `ipPin`, `ein` and any explicit `dependent*SSN`/`dependent*ITIN` answer: AES-256-GCM (`SSN_ENCRYPTION_KEY`); legacy plaintext is re-encrypted when the owner next reads or saves (`lib/tax-organizer`). `dependentDetails`: SSN/ITIN/EIN-shaped digits replaced with `[redacted-id]` before storage; PDFs print masked values only | Server API for the owner | Until deletion |
| Imported W-2 / 1099 / platform summaries incl. employer EIN | Firestore `w2_income`, `income_1099`, `gross_receipts` | Google default encryption; plaintext fields | Owner; server | Until deletion |
| Billing: Stripe customer id, subscription status | `user_profiles` (server-written); Stripe | Google default; Stripe-managed | Owner read; server | WriteOff side until deletion; Stripe retains under its own policy |
| Support requests to a CPA (`cpa_questions`, email via Resend) | Firestore; operator mailbox | Google default; mailbox provider | Server; operator | Candidate account deletion includes owned `cpa_questions`; mailbox retention remains an operator procedure |
| Deletion marker `account_deletions/{uid}` | Firestore | Google default | Server only | Retained after deletion by design |
| Support access audit `support_audit` | Firestore | Google default | Server only | Retained; no expiry in code |
| Rate-limit counters (hashed keys) | Firestore `rate_limits` | Google default | Server only | No expiry logic in code |
| Operational logs (userId, endpoint, status, error text) | Cloud Logging via `console.*` | Google-managed | Project IAM members | 30 days (`_Default` bucket default); 400 days for `_Required` audit logs |

### 3.2 Foreseeable threats and current controls

| Threat | Controls in place | Residual risk |
|---|---|---|
| Credential stuffing / phishing of users | Email verification required before API access; IP-scoped session-creation limit (30 per 10 min); Firebase Auth password policy (Google-managed, configuration unverified) | High: no MFA for users (G2) |
| Compromise of an operator Google or GitHub account | Deploy requires manual `workflow_dispatch` with the exact 40-character commit and a typed confirmation. Project IAM verified 2026-09-21: three human principals (one Owner, two Editors) and six service accounts; no other humans hold project access | High: MFA on those accounts is operator-configured, unverified (G2); the two Editor grants exceed least privilege (G5) |
| Support-tool misuse | `SUPPORT_ADMIN_UIDS` allowlist AND `admin` custom claim; redacted diagnostics only (forbidden keys: token, secret, ssn, cursor, amount, password, private, encrypted); every lookup written to `support_audit`; 60 lookups per 10 min | Medium: raw Firestore console access by project IAM members is not logged at the application layer (G6) |
| Bank-token theft from a database export | Tokens are ciphertext without `PLAID_TOKEN_ENCRYPTION_KEY`; key held outside Firestore. The completed migration moved legacy credentials into encrypted server-only records and preserved account and transaction state | Low for database-only exposure; no documented rotation procedure (G4) |
| Leakage of identifiers to the AI vendor | `store: false` on every OpenAI call; every string in the transaction-analysis payload is passed through the shared identifier redactor (`redactIdentifierStrings`: SSN/ITIN/EIN shapes, bare nine-digit runs including after an OCR-misread colon; amounts preserved), so new free-text fields are covered by default; no SSN field is sent from transaction analysis. W-2/1099/platform-summary imports are read by local Tesseract OCR first and only the text, with SSN/ITIN/EIN-shaped numbers replaced by `[redacted-id]`, goes to the model (`lib/ocr/document-text.ts`, `lib/security/identifier-redaction.ts`); the whole image is sent only under a signed §7216 consent (`consents.document_import`) plus a per-document authorization, else 403 `DOCUMENT_IMAGE_CONSENT_REQUIRED` | Medium: bank/credit-card statement images (`import-bank-statement`) are still sent whole to GPT-4o vision (G3) |
| Firestore rules misconfiguration | Rules enforce UID ownership; server-only collections deny all client access; release and candidate rule suites exercise Firestore and Storage against local emulators | Low |
| Cross-site request forgery | `lib/security/request-origin.ts` rejects requests whose Origin/Referer is not a trusted application host; SameSite cookies | Low |
| Malicious uploads | 10 MB limit, content-type and byte-signature checks, verified-email authentication and transaction ownership; candidate Storage rules deny direct client receipt I/O | Low; signature checks are not malware scanning |
| Key or secret exposure in code | Secrets are read from environment; CI does not print them; release scripts refuse to write private outputs inside the checkout | Medium: no secret scanning configured in the repository (unverified whether GitHub secret scanning is enabled) |
| Accidental deletion or corrupt writes | Deletion is transactional and refuses to finish when cleanup fails. Production PITR and delete protection are enabled; one daily managed backup schedule has 14-week retention; an isolated export restore preserved structure and deny-all access | Medium: the first completed managed backup and a restore from that managed backup have not yet been observed (G7) |

Reassess this table when a new data class, vendor, or region is added, and at least annually.

## 4. Safeguards (§314.4(c)) mapped to actual controls

1. Access controls (§314.4(c)(1)). The procedure is `docs/compliance/ACCESS_CONTROL_POLICY_2026-09-21.md`. Authentication is Firebase Auth (email/password with mandatory email verification, or Google sign-in). Every API route calls `getAuthenticatedUser`, which verifies a Firebase ID token or the `__session` cookie with revocation checking. Authorization is UID ownership in Firestore and Storage rules; `plaid_connections`, `account_deletions`, `rate_limits`, and `support_audit` are server-only. Support administrators must be both allowlisted in `SUPPORT_ADMIN_UIDS` and carry `admin: true`. Periodic access review: none documented (G5).
2. Data and system inventory (§314.4(c)(2)). §3.1 above and `docs/compliance/SUBPROCESSORS_2026-09-17.md`. Update both when the schema or vendor set changes.
3. Encryption (§314.4(c)(3)). In transit: HTTPS everywhere; TLS 1.0 and 1.1 are refused, TLS 1.2 is the floor and TLS 1.3 is negotiated by default; HSTS and browser security headers are set. At rest: Google default encryption for Firestore, Storage, Auth and logs; application-layer AES-256-GCM for Plaid access tokens, organizer identifiers and candidate profile EIN storage. Identifier-shaped digits are removed from transaction-analysis and assistant/voice text before model calls. Not encrypted at field level: imported W-2/1099 employer or payer EIN fields (G3). `PLAID_TOKEN_ENCRYPTION_KEY` and `SSN_ENCRYPTION_KEY` are distinct production values held outside the repository. No rotation or escrow procedure exists yet (G4).
4. Secure development (§314.4(c)(4)). CI runs on every pull request and `codex/**` push using fresh installs, runtime audits, lint, tests, an isolated web build, TypeScript and both worker builds. Production preflight rejects test credentials, emulator flags, missing release secrets and the wrong provider account. Live release `a219cbe` passed 6,088 tests in 236 files plus separate rules/reconnect/restoration suites. Source runtime and final generated SSR audits recorded zero high and zero critical findings; the generated SSR package retained nine moderate findings. No SAST step, scan of employee/contractor machines, external penetration test, or written patch SLA is documented (G8).
5. Multi-factor authentication (§314.4(c)(5)). Users: not enforced. The codebase never enrolls or requires a second factor; Firebase Auth MFA requires the Identity Platform upgrade and enrollment code, neither of which is present. The only MFA-aware code reads the `sign_in_second_factor` claim to gate the filing sandbox (`app/api/tax/filing/route.ts`). Support administrators: no second factor required by the application. Operator consoles (Google Cloud, Firebase, GitHub, Stripe, Plaid, OpenAI): MFA is configurable by the operator and is unverified. No written Qualified Individual approval of an equivalent control exists. This is the largest Safeguards Rule gap (G2) and also fails IRS Pub 1345's MFA expectation for online providers should WriteOff transmit returns.
6. Secure disposal (§314.4(c)(6)). User-initiated deletion (`lib/firebase/delete-user-data.ts`) revokes bank access at Plaid, cancels Stripe subscriptions and deletes the Stripe customer, deletes receipt objects, deletes owned Firestore collections and the profile tree, then deletes the Auth user. No automatic disposal of inactive accounts after two years of non-use exists (G10). Schedule: `docs/compliance/DATA_RETENTION_AND_DELETION_2026-09-17.md`.
7. Change management (§314.4(c)(7)). Changes reach production only through `.github/workflows/deploy.yml`, a manual `workflow_dispatch` that checks out the exact reviewed commit and requires typing `deploy:writeoff-23910:<release_commit>`; the release script refuses to deploy from a dirty tree, verifies every file against the reviewed commit's blob ids, requires a signed migration-review record, and refuses any Node major other than 22. Rules and indexes deploy with the release (the composite indexes the release needs were deployed additively on 2026-09-21). Branch protection and mandatory review on `march-branch` are GitHub settings that could not be verified from the repository (unverified).
8. Activity monitoring (§314.4(c)(8)). Support reads are logged to `support_audit`; scheduled syncs write a monitored heartbeat; application errors go to Cloud Logging. Five enabled alert policies cover regional availability, SSR errors, background errors, absent sync completions and aging unacknowledged analysis messages, and alert delivery was verified. Firestore/Storage Data Access audit logs are still not documented as enabled (G6).

## 5. Testing and monitoring (§314.4(d))

Automated: CI, rules-emulator tests, runtime dependency audits, deployment artifact verification, uptime/error/background/heartbeat/backlog alerts and Dependabot. Not present: an external annual penetration test, vulnerability scans of employee/contractor machines, and SAST (G8). Alerting does not prove every acknowledged job finishes and is not a substitute for periodic security assessment.

## 6. Training (§314.4(e))

No security-awareness training or acknowledgment records exist (G5). Minimum program: annual review of this WISP, the incident response plan, and IRS Pub 4557 by everyone with production or support access; signed acknowledgment kept by the Qualified Individual; specialized training for whoever holds the encryption keys and Firebase admin roles.

## 7. Service provider oversight (§314.4(f))

Vendors, data categories, and agreement links are in `docs/compliance/SUBPROCESSORS_2026-09-17.md`. Required: confirm the Google Cloud, Stripe, Plaid, OpenAI, and Resend data processing terms are accepted under WriteOff's accounts and keep dated copies; review each vendor annually; give any contractor that receives tax return information the written §7216/§6713 notice required by Treas. Reg. §301.7216-2(d)(2) (G11).

## 8. Program maintenance (§314.4(g)) and incident response (§314.4(h))

Review triggers: new data class, new vendor, new region, a security event, a failed test, or a change in law. Incident response: `docs/compliance/INCIDENT_RESPONSE_PLAN_2026-09-17.md`, which includes FTC notification within 30 days for events affecting 500 or more consumers (§314.4(j)).

## 9. Annual report (§314.4(i))

The Qualified Individual reports in writing at least annually to the owner or governing body covering: overall compliance with this program; the risk assessment and control decisions; vendor arrangements; test results; security events and responses; and recommended changes. Keep reports with the operator appendix.

## 10. Gaps and owners

| ID | Gap | Owner | Minimum fix |
|---|---|---|---|
| G1 | Qualified Individual designated 2026-09-21 (closed); annual report not yet produced | Qualified Individual | First annual report due 2027-09 |
| G2 | No MFA for users, support admins, or (verified) operator consoles | Engineering (users/admins), Operator (consoles) | Enable Identity Platform MFA; require enrollment for `admin` claim holders first, then all users; enforce MFA on every console account |
| G3 | Candidate profile EIN and organizer identifiers are encrypted; imported W-2/1099 employer/payer EIN fields remain plaintext. Bank-statement images and consented document images can be sent to OpenAI vision | Engineering | Encrypt remaining imported EIN fields; minimize/redact image regions before provider transfer where feasible |
| G4 | No key rotation or escrow procedure for `PLAID_TOKEN_ENCRYPTION_KEY` / `SSN_ENCRYPTION_KEY` | Engineering + Operator | Write and test re-encryption tooling; store keys in Secret Manager with an escrow copy |
| G5 | No documented access review, no training records; two project Editors beyond the Owner | Operator | Reduce the Editor grants to the least role needed; quarterly access review; annual training with signed acknowledgments |
| G6 | Production alerting is live; Firestore/Storage Data Access audit logging is not documented as enabled | Engineering + Operator | Enable and verify Data Access logs with retention/cost review |
| G7 | PITR, delete protection and a daily 14-week backup schedule are live; first managed backup completion/restore is unverified | Operator | Observe a completed managed backup and perform a restricted restore drill |
| G8 | Live source and generated runtime audits have 0 high/critical findings; nine generated-runtime moderate findings remain. No laptop scanning, SAST, external penetration test, or patch SLA | Engineering + Operator | Track moderate upgrades; set a patch SLA; schedule an external test and endpoint scanning |
| G9 | Candidate account deletion removes `cpa_questions`; operator mailbox copies follow the mailbox retention procedure | Operator | Verify deletion in staging and document/automate mailbox purge |
| G10 | No disposal of inactive accounts | Engineering + Counsel | Define inactivity period and notice; implement purge |
| G11 | No signed DPA inventory; no §7216 contractor notices | Operator + Counsel | Collect executed terms; issue notices |
| G12 | Terms of Service and required acknowledgment are live; scope says planning only, no filing/advice — engineering item closed | Counsel | Counsel must confirm governing law, limitation language and launch disclosures |

## Sources

- 16 CFR Part 314 (Safeguards Rule): https://www.ecfr.gov/current/title-16/chapter-I/subchapter-C/part-314 (automated retrieval blocked; elements confirmed via the FTC guide below)
- FTC, "FTC Safeguards Rule: What Your Business Needs to Know" (Dec 2024): https://www.ftc.gov/business-guidance/resources/ftc-safeguards-rule-what-your-business-needs-know
- FTC Safeguards Rule page: https://www.ftc.gov/legal-library/browse/rules/safeguards-rule
- IRS Pub 4557, Safeguarding Taxpayer Data: https://www.irs.gov/pub/irs-pdf/p4557.pdf
- IRS Pub 5708, Creating a Written Information Security Plan: https://www.irs.gov/pub/irs-pdf/p5708.pdf
- IRS Pub 1345 (online provider security standards, MFA): https://www.irs.gov/pub/irs-pdf/p1345.pdf
- Treas. Reg. §301.7216-1 and -2 (software developers as preparers; contractor notice): https://www.ecfr.gov/current/title-26/section-301.7216-1 ; https://www.ecfr.gov/current/title-26/section-301.7216-2
- Firebase Auth multi-factor (requires Identity Platform): https://firebase.google.com/docs/auth/web/multi-factor
- Cloud Logging retention defaults: https://cloud.google.com/logging/quotas
- Firestore point-in-time recovery: https://cloud.google.com/firestore/docs/pitr
- Prior internal research: `docs/research/reporting-state-compliance-2026.md` §4.3–4.4

## Facts verified in code

- `firestore.rules`: UID-scoped reads; server-only `plaid_connections`, `account_deletions`, `rate_limits`, `support_audit`; client cannot alter subscription/consent fields; `user_corrections` immutable.
- `storage.rules`, `lib/firebase/receipt-security.ts`: receipts under `receipts/{uid}/`, 10 MB limit, content checks, `verifySessionCookie(session, true)`.
- `lib/plaid/connections.ts`: `aes-256-gcm`, `PLAID_TOKEN_ENCRYPTION_KEY`, Admin SDK only.
- `lib/security/utils.ts`, `lib/tax-organizer/identifiers.ts`, `app/api/tax/organizer/route.ts`: `aes-256-gcm` with `SSN_ENCRYPTION_KEY`; `ORGANIZER_IDENTIFIER_FIELDS` = taxpayerSSN, spouseSSN, bankAccount, bankRouting, ipPin, ein plus `dependent*SSN`/`dependent*ITIN`; `dependentDetails` redacted with `lib/security/identifier-redaction.ts`; legacy plaintext re-encrypted on read (`lib/tax-organizer/organizer-server.ts`); `scripts/organizer-identifier-audit.mjs --report` counts remaining plaintext read-only.
- `lib/firebase/profiles.ts`: `ein` stored on the profile without encryption.
- `lib/firebase/api-auth.ts`: `verifyIdToken(token, true)`, `verifySessionCookie(session, true)`, email-verified check, `secondFactorVerified`, `admin` claim.
- `app/api/auth/session/route.ts`, `app/api/auth/logout/route.ts`: 14-day cookie; logout clears cookies.
- `lib/security/rate-limit.ts`: limits for session, receipts, AI analysis, export, delete, Stripe, Plaid link, support lookup.
- `lib/security/request-origin.ts`: trusted-origin enforcement.
- `lib/support/access.ts`, `lib/support/account-diagnostics.ts`: allowlist + claim; `support_audit`; forbidden keys.
- `lib/ai/analyzeTransaction.ts`, `lib/openai/client.ts`, `app/api/tax/import-document/route.ts`, `app/api/tax/import-bank-statement/route.ts`: `store: false`; `redactTaxIdentifiers`; image inputs.
- `lib/firebase/delete-user-data.ts`: ordered deletion; retained `account_deletions`.
- `lib/error-logger.ts`: console output only (Cloud Logging).
- `middleware.ts`: HSTS, CSP, frame, referrer, permissions headers; GA origins only when configured.
- `.github/workflows/ci.yml`, deployment scripts and vendored Firebase adapter integrity checks: all-PR/codex CI, fresh runtime audits, isolated build and exact-artifact deployment verification. Live generated runtime: 0 high, 0 critical, 9 moderate.
- `functions/src/index.ts`, `functions-analysis/src/index.ts`, `firebase.json`: scheduled sync; `us-central1`.
- `app/api/tax/filing/route.ts`, `lib/tax-filing/config.ts`: filing is sandbox-only; no return is transmitted.
- Verified on the live project 2026-09-24: TLS/HSTS, PITR, delete protection, one daily 14-week managed backup schedule, five alert policies, seven active functions, 47 ready indexes, two schedules and the exact coordinated release.
- Absent from the codebase or operator record: consumer/support MFA enrollment, training records, SAST, endpoint scanning and an external penetration test.
