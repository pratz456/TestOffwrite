import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Finish bank sign-in', robots: { index: false, follow: false }, referrer: 'no-referrer' };
export default function PlaidOAuthLayout({ children }: { children: React.ReactNode }) { return children; }
